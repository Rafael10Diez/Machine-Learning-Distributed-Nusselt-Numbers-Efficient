./machine_learning/source/models
    a) Neural Network models available for the study.
        - Only one model is chosen by the final runner (cnn_alt2.py).
        - The remaining two models are stored for legacy reasons.